import React, { useEffect, useState } from "react";
import { Button, Card } from "react-bootstrap";
import { useParams } from "react-router-dom";

const CatalogueDetails = (props) => <div></div>

export default CatalogueDetails;
